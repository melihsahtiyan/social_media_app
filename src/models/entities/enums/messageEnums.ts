export enum MessageTypes {
	TEXT = 'text',
	MEDIA = 'media',
	POST = 'post',
	EVENT = 'event',
}

export enum MessageStatus {
	SENT = 'sent',
	DELIVERED = 'delivered',
	READ = 'read',
	DELETED = 'deleted',
}
